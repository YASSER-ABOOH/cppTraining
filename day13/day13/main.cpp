#include<cstring>
#include<iostream>

using namespace std;

int main()
{
	char Line[] = "This is a Cpp Programming Class";
	char = 'P';
	char* res = strchr(line, ch);
	cout << res << endl;
	return 0;
}

//I want to fetch the delimiter to search the input buffer until the null character.